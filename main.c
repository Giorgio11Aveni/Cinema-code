#include "data.h"

int main()
{

    int calendario[31];



















    //int input; //variabile che assumerà il valore della scelta che l'utente digiterà

    /*richiamo le funzioni che all'interno della variabili lista e lista2 (rispettivamente di tipo prenotazione e sala) mi inseiranno tutte le prenotazioni e le
     disposizioni delle sale (creo e carico le liste concatenate)*/
    /*sala lista2 = carica_sala("sale.txt", lista2);
    prenotazione lista = carica_prenotazione("prenotazioni.txt", lista);*/


    /*creo una variabile di tipo spettacoli che successivamente diventerà il mio array di struct*/
    /*spettacolo calendario_spettacoli;*/

    /* richiamo la funzione che mi creera un array vuoto di 31 struct */
    /*calendario_spettacoli= inizializza_c(DIM);*/

    /*richiamo la funzione che andrà a caricarmi tutti i giorni del calendario con rispettivi spettacoli all'interno del array precedentemente creato */
    /*carica_spettacolo("spettacoli.txt", calendario_spettacoli);*/


    /*creo un menú da mostrare all'utente dandogli la possibilità di effettuare tre scelte.
        do{
                printf("----------MENU----------\n");
                printf("Inserisci un numero da 1 a 3 per effettuare una delle seguenti scelte (un altro numero per uscire): \n");
                printf("-Inserisci 1 per effettuare una prenotazione\n");
                printf("-Inserisci 2 per trasformare le richieste in prenotazioni\n");
                printf("-Inserisci 3 per visualizzare i posti liberi di ogni sala\n");
                scanf("%d", &input);

            switch(input){


                case 1:

                    lista = inserisci_prenotazione(lista, lista2); /*richiamo la funzione che permetterà all'utente di inserire una nuova prenotazione

                    break;

                case 2:

                    lista = inserisci_da_file("richieste.txt", lista, lista2); /* richiamo la funzione che permetterà all'utente di trasormare le richieste in prenotazioni

                    break;

                case 3:

                    disponibili(lista2); /*richiamo la funzione che farà vedere all'utente i tavoli disponibili in ogni sala

                    break;
                    default: input=0;

            }

        }

        while (input!=0);

        return EXIT_SUCCESS;*/

}

